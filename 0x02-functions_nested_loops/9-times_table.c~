#include "main.h"
/**
 * times_table - oui
 *
 * 
 */


void times_table(void)
{

  int i, num;
  num = 0;
  if (num <= 9)
    {
      for(i = 1; i <= 9; i++)
	{
	  _putchar(num*i);  
	}
  ++num;
    }
}
