#include "main.h"

/**
 * print_sign - sign for number
 *
 * @n: variable
 *
 * Return: 1 is lower and upper case 0 to the other
 */

int _abs(int n )
{
  
    if (n < 0)
	{
		n = n * -1;
	}
	return (n);
}
