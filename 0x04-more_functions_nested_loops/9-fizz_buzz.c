#include <stdio.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    int num = 0;
	for (num = 0; num <= 100; num++)
	{
		if (num % 5 == 0 || num % 3 == 0)
		{
			printf("FizzBuzz");
		}
		else if (num )
		{
			/* code */
		}
		
		
		printf("%d" , num);

	}
	
}