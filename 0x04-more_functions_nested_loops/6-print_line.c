#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_line(int n)
{
    int a = 0;
    for (a = 0; a <= n; a++)
    {
        _putchar(95);
    }
    _putchar('\n');

}